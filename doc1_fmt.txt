PHASED DEVELOPMENT PLANSecure Wireless Communication using Lightweight Cryptography6 Phases  •  42 Actionable Tasks  •  Complete Code Patterns  •  ChecklistsEach phase builds on the previous one. Complete every checklist item before moving forward.March 2026Development Roadmap OverviewThe entire project is divided into 6 self-contained phases. Each phase has a clear objective, a list of tasks with checklists, exact code to write, and a verification step so you know when you are truly done before moving to the next phase.PhaseNameDurationWhat You BuildExit Criteria1Foundation & EnvironmentDay 1-2Project skeleton, dependencies, toolingAll imports work, ASCON test passes2Cryptographic EnginesDay 3-5ASCON, AES, SPECK, PRESENT wrappersAll 4 engines pass encrypt/decrypt + KAT tests3Networking & ProtocolDay 6-8Sender, receiver, packet format, replay guardEncrypted message sent & received over TCP socket4Benchmarking SuiteDay 9-11Benchmark runner, metrics collection, CSV exportCSV with timing data for all 4 algos x 6 payloads5Attack SimulationsDay 12-14Eavesdrop, replay, MITM demos3 attack scripts producing visual proof of defense6Analysis, Visualization & ReportDay 15-18Charts, analysis, final report/presentation6 charts + complete project reportTotal estimated time: 15-18 working days (or 4-5 weekends if done part-time). Each phase is designed to be completable in 2-3 focused sessions of 3-4 hours each.`nPHASE 1Foundation & Environment Setup`nDuration: Day 1-2  •  Objective: Go from zero to a working project skeleton where every dependency is installed and verified`n1.1 Create Project StructureOpen a terminal and run these commands one by one:mkdir -p secure-wireless-lwc/src/{crypto,network,benchmark,attacks,utils}mkdir -p secure-wireless-lwc/{tests,results,docs}cd secure-wireless-lwcpython -m venv venvsource venv/bin/activate        # Linux/Macvenv\Scripts\activate           # Windows`n1.2 Install All DependenciesCreate requirements.txt with the following content, then install:ascon>=0.0.9pycryptodome>=3.20.0scapy>=2.5.0matplotlib>=3.8.0seaborn>=0.13.0pandas>=2.1.0numpy>=1.26.0psutil>=5.9.0pip install -r requirements.txt`n1.3 Verify ASCON InstallationCreate a file called verify_setup.py and run it. Every line should print without error:# verify_setup.pyimport asconfrom Crypto.Cipher import AESimport scapy.allimport matplotlibimport pandasimport numpyimport tracemallocimport socketimport structimport os, time# Quick ASCON smoke testkey = os.urandom(16)nonce = os.urandom(16)plaintext = b'Hello, Lightweight Crypto!'ciphertext = ascon.encrypt(key, nonce, b'', plaintext)decrypted = ascon.decrypt(key, nonce, b'', ciphertext)assert decrypted == plaintext, 'ASCON FAILED!'print('ALL IMPORTS OK. ASCON encrypt/decrypt VERIFIED.')`n1.4 Create Base FilesCreate empty __init__.py files in every package directory and the abstract base class:touch src/__init__.py src/crypto/__init__.py src/network/__init__.pytouch src/benchmark/__init__.py src/attacks/__init__.py src/utils/__init__.pyCreate src/crypto/base_engine.py:from abc import ABC, abstractmethodclass CryptoEngine(ABC):    @abstractmethod    def encrypt(self, key: bytes, nonce: bytes, ad: bytes, plaintext: bytes) -> tuple[bytes, bytes]:        '''Returns (ciphertext, auth_tag)'''        pass    @abstractmethod    def decrypt(self, key: bytes, nonce: bytes, ad: bytes, ciphertext: bytes, tag: bytes) -> bytes | None:        '''Returns plaintext on success, None on auth failure'''        pass    @abstractmethod    def name(self) -> str:        pass    @property    def algo_id(self) -> int:        '''Unique ID for packet header: 1=ASCON, 2=AES, 3=SPECK, 4=PRESENT'''        return 01.5 `nPhase 1 ChecklistDoneTaskFile(s) to CreateVerification☐Create full directory structureAll dirs under secure-wireless-lwc/ls -R shows all folders☐Create and activate virtual environmentvenv/which python points to venv☐Install all pip dependenciesrequirements.txtpip list shows all packages☐Run verify_setup.py successfullyverify_setup.pyPrints ALL IMPORTS OK message☐Create base_engine.py abstract classsrc/crypto/base_engine.pypython -c 'from src.crypto.base_engine import CryptoEngine' works☐Create all __init__.py filessrc/*/__init__.pyNo import errors from any package☐Initialize git repository.gitignore, README.mdgit log shows initial commit`nExit Gate: Run verify_setup.py. If it prints the success message, `nPhase 1 is complete. Do NOT proceed until this works.`nPHASE 2Cryptographic Engines`nDuration: Day 3-5  •  Objective: Implement all 4 cipher wrappers and prove each one works with unit tests`n2.1 ASCON Engine (Primary Algorithm)Create src/crypto/ascon_engine.py:import ascon as _asconfrom .base_engine import CryptoEngineclass AsconEngine(CryptoEngine):    def encrypt(self, key, nonce, ad, plaintext):        # ascon.encrypt returns ciphertext || 16-byte tag appended        ct_tag = _ascon.encrypt(key, nonce, ad, plaintext)        return ct_tag[:-16], ct_tag[-16:]   # (ciphertext, tag)    def decrypt(self, key, nonce, ad, ciphertext, tag):        result = _ascon.decrypt(key, nonce, ad, ciphertext + tag)        return result   # None on failure, plaintext on success    def name(self):        return 'ASCON-AEAD128'    @property    def algo_id(self):        return `n1`n2.2 AES-128-GCM Engine (Industry Baseline)Create src/crypto/aes_engine.py:from Crypto.Cipher import AESfrom .base_engine import CryptoEngineclass AESEngine(CryptoEngine):    def encrypt(self, key, nonce, ad, plaintext):        # AES-GCM uses 12-byte nonce for best performance        cipher = AES.new(key[:16], AES.MODE_GCM, nonce=nonce[:12])        cipher.update(ad)        ct, tag = cipher.encrypt_and_digest(plaintext)        return ct, tag    def decrypt(self, key, nonce, ad, ciphertext, tag):        try:            cipher = AES.new(key[:16], AES.MODE_GCM, nonce=nonce[:12])            cipher.update(ad)            pt = cipher.decrypt_and_verify(ciphertext, tag)            return pt        except (ValueError, KeyError):            return None   # Auth failure    def name(self):        return 'AES-128-GCM'    @property    def algo_id(self):        return `n2`n2.3 SPECK-128/128 Engine (Lightweight Comparison)Since there is no standard SPECK AEAD library in Python, implement the raw SPECK block cipher and wrap it in CTR mode + HMAC for authenticated encryption. Create src/crypto/speck_engine.py:import struct, hmac, hashlib, osfrom .base_engine import CryptoEngineclass SpeckCipher:    '''SPECK 128/128: 128-bit block, 128-bit key, 32 rounds'''    ROUNDS = 32    WORD_SIZE = 64    MOD = 2 ** 64    def __init__(self, key_bytes):        assert len(key_bytes) == 16        # Parse key as two 64-bit words (big-endian)        self.key_schedule = self._expand_key(key_bytes)    def _expand_key(self, key_bytes):        b, a = struct.unpack('>QQ', key_bytes)  # l[0], k[0]        k = [a]        l = [b]        for i in range(self.ROUNDS - 1):            new_l = (k[i] + self._ror(l[i], 8)) % self.MOD            new_l ^= i            new_k = self._rol(k[i], 3) ^ new_l            l.append(new_l)            k.append(new_k)        return k    def _ror(self, x, r):        return ((x >> r) | (x << (self.WORD_SIZE - r))) % self.MOD    def _rol(self, x, r):        return ((x << r) | (x >> (self.WORD_SIZE - r))) % self.MOD    def encrypt_block(self, block_bytes):        y, x = struct.unpack('>QQ', block_bytes)        for i in range(self.ROUNDS):            x = (self._ror(x, 8) + y) % self.MOD            x ^= self.key_schedule[i]            y = self._rol(y, 3) ^ x        return struct.pack('>QQ', y, x)    def decrypt_block(self, block_bytes):        y, x = struct.unpack('>QQ', block_bytes)        for i in range(self.ROUNDS - 1, -1, -1):            y = self._ror(y ^ x, 3)            x = self._rol((x ^ self.key_schedule[i]) - y % self.MOD, 8)        return struct.pack('>QQ', y, x)class SpeckEngine(CryptoEngine):    '''SPECK-128/128 in CTR mode + HMAC-SHA256 for AEAD'''    BLOCK_SIZE = 16    def encrypt(self, key, nonce, ad, plaintext):        cipher = SpeckCipher(key[:16])        # CTR mode encryption        ct = bytearray()        num_blocks = (len(plaintext) + self.BLOCK_SIZE - 1) // self.BLOCK_SIZE        for i in range(num_blocks):            counter = nonce[:8] + struct.pack('>Q', i)            keystream = cipher.encrypt_block(counter)            start = i * self.BLOCK_SIZE            block = plaintext[start:start + self.BLOCK_SIZE]            ct.extend(b ^ k for b, k in zip(block, keystream[:len(block)]))        # HMAC tag over ad + ct        mac_data = ad + bytes(ct)        tag = hmac.new(key, mac_data, hashlib.sha256).digest()[:16]        return bytes(ct), tag    def decrypt(self, key, nonce, ad, ciphertext, tag):        # Verify HMAC first        mac_data = ad + ciphertext        expected_tag = hmac.new(key, mac_data, hashlib.sha256).digest()[:16]        if not hmac.compare_digest(tag, expected_tag):            return None        # CTR mode decryption (same as encryption)        cipher = SpeckCipher(key[:16])        pt = bytearray()        num_blocks = (len(ciphertext) + self.BLOCK_SIZE - 1) // self.BLOCK_SIZE        for i in range(num_blocks):            counter = nonce[:8] + struct.pack('>Q', i)            keystream = cipher.encrypt_block(counter)            start = i * self.BLOCK_SIZE            block = ciphertext[start:start + self.BLOCK_SIZE]            pt.extend(b ^ k for b, k in zip(block, keystream[:len(block)]))        return bytes(pt)    def name(self): return 'SPECK-128/128-CTR-HMAC'    @property    def algo_id(self): return `n3`n2.4 PRESENT-80 Engine (Ultra-Lightweight Comparison)Create src/crypto/present_engine.py. PRESENT uses a 64-bit block and 80-bit key. Since 80-bit keys offer only 80-bit security (lower than the others), this is included to show the trade-off:import struct, hmac, hashlibfrom .base_engine import CryptoEngine# PRESENT S-box (4-bit)SBOX = [0xC, 0x5, 0x6, 0xB, 0x9, 0x0, 0xA, 0xD,        0x3, 0xE, 0xF, 0x8, 0x4, 0x7, 0x1, 0x2]SBOX_INV = [SBOX.index(i) for i in range(16)]def sub_nibbles(state):    result = 0    for i in range(16):        nibble = (state >> (i * 4)) & 0xF        result |= SBOX[nibble] << (i * 4)    return resultdef p_layer(state):    result = 0    for i in range(64):        if state & (1 << i):            pos = (i * 16) % 63 if i != 63 else 63            result |= 1 << pos    return resultdef generate_round_keys(key_80bit):    '''Generate 32 round keys from 80-bit key register'''    keys = []    key_reg = key_80bit    for i in range(1, 33):        keys.append(key_reg >> 16)  # top 64 bits        # Rotate left by 61        key_reg = ((key_reg << 61) | (key_reg >> 19)) & ((1 << 80) - 1)        # S-box on top 4 bits        top = SBOX[(key_reg >> 76) & 0xF]        key_reg = (key_reg & ((1 << 76) - 1)) | (top << 76)        # XOR round counter into bits 15-19        key_reg ^= (i << 15)    return keysdef present_encrypt_block(plaintext_64, key_80bit):    round_keys = generate_round_keys(key_80bit)    state = plaintext_64    for i in range(31):        state ^= round_keys[i]        state = sub_nibbles(state)        state = p_layer(state)    state ^= round_keys[31]    return stateclass PresentEngine(CryptoEngine):    '''PRESENT-80 in CTR mode + HMAC-SHA256'''    BLOCK_SIZE = 8  # 64-bit block    def encrypt(self, key, nonce, ad, plaintext):        # Use first 10 bytes (80 bits) as PRESENT key        key_80 = int.from_bytes(key[:10], 'big')        ct = bytearray()        num_blocks = (len(plaintext) + self.BLOCK_SIZE - 1) // self.BLOCK_SIZE        for i in range(num_blocks):            ctr_val = int.from_bytes(nonce[:4] + struct.pack('>I', i), 'big')            ks = present_encrypt_block(ctr_val, key_80)            ks_bytes = ks.to_bytes(8, 'big')            start = i * self.BLOCK_SIZE            block = plaintext[start:start + self.BLOCK_SIZE]            ct.extend(b ^ k for b, k in zip(block, ks_bytes[:len(block)]))        tag = hmac.new(key[:16], ad + bytes(ct), hashlib.sha256).digest()[:16]        return bytes(ct), tag    def decrypt(self, key, nonce, ad, ciphertext, tag):        expected = hmac.new(key[:16], ad + ciphertext, hashlib.sha256).digest()[:16]        if not hmac.compare_digest(tag, expected):            return None        # CTR decryption identical to encryption        key_80 = int.from_bytes(key[:10], 'big')        pt = bytearray()        for i in range((len(ciphertext) + self.BLOCK_SIZE - 1) // self.BLOCK_SIZE):            ctr_val = int.from_bytes(nonce[:4] + struct.pack('>I', i), 'big')            ks = present_encrypt_block(ctr_val, key_80)            ks_bytes = ks.to_bytes(8, 'big')            start = i * self.BLOCK_SIZE            block = ciphertext[start:start + self.BLOCK_SIZE]            pt.extend(b ^ k for b, k in zip(block, ks_bytes[:len(block)]))        return bytes(pt)    def name(self): return 'PRESENT-80-CTR-HMAC'    @property    def algo_id(self): return `n4`n2.5 Unit Test FileCreate tests/test_crypto_engines.py:import os, pytestfrom src.crypto.ascon_engine import AsconEnginefrom src.crypto.aes_engine import AESEnginefrom src.crypto.speck_engine import SpeckEnginefrom src.crypto.present_engine import PresentEngineENGINES = [AsconEngine(), AESEngine(), SpeckEngine(), PresentEngine()]@pytest.mark.parametrize('engine', ENGINES, ids=lambda e: e.name())def test_encrypt_decrypt_roundtrip(engine):    key, nonce, ad = os.urandom(16), os.urandom(16), b'test-ad'    for size in [0, 1, 15, 16, 17, 64, 256, 1024]:        pt = os.urandom(size)        ct, tag = engine.encrypt(key, nonce, ad, pt)        result = engine.decrypt(key, nonce, ad, ct, tag)        assert result == pt, f'{engine.name()} failed at size {size}'@pytest.mark.parametrize('engine', ENGINES, ids=lambda e: e.name())def test_wrong_key_fails(engine):    key, nonce, ad, pt = os.urandom(16), os.urandom(16), b'ad', b'secret'    ct, tag = engine.encrypt(key, nonce, ad, pt)    wrong_key = os.urandom(16)    result = engine.decrypt(wrong_key, nonce, ad, ct, tag)    assert result is None, f'{engine.name()} should fail with wrong key'@pytest.mark.parametrize('engine', ENGINES, ids=lambda e: e.name())def test_tampered_ciphertext_fails(engine):    key, nonce, ad, pt = os.urandom(16), os.urandom(16), b'ad', b'hello world'    ct, tag = engine.encrypt(key, nonce, ad, pt)    tampered = bytearray(ct)    tampered[0] ^= 0xFF  # Flip bits in first byte    result = engine.decrypt(key, nonce, ad, bytes(tampered), tag)    assert result is None, f'{engine.name()} should detect tampered ciphertext'@pytest.mark.parametrize('engine', ENGINES, ids=lambda e: e.name())def test_tampered_ad_fails(engine):    key, nonce, ad, pt = os.urandom(16), os.urandom(16), b'real-ad', b'msg'    ct, tag = engine.encrypt(key, nonce, ad, pt)    result = engine.decrypt(key, nonce, b'fake-ad', ct, tag)    assert result is None, f'{engine.name()} should detect tampered AD'Run tests with: pytest tests/test_crypto_engines.py -v2.6 `nPhase 2 ChecklistDoneTaskFile(s) to CreateVerification☐Implement AsconEnginesrc/crypto/ascon_engine.pyRoundtrip test passes☐Implement AESEnginesrc/crypto/aes_engine.pyRoundtrip test passes☐Implement SpeckEngine with SPECK block ciphersrc/crypto/speck_engine.pyRoundtrip test passes☐Implement PresentEngine with PRESENT block ciphersrc/crypto/present_engine.pyRoundtrip test passes☐Write test_crypto_engines.py with 4 test typestests/test_crypto_engines.pyAll 16 tests pass (4 engines x 4 tests)☐Test with wrong key returns None for all engines-pytest output shows PASSED☐Test tampered ciphertext returns None for all engines-pytest output shows PASSED☐Test tampered associated data returns None for all engines-pytest output shows PASSED`nExit Gate: pytest tests/test_crypto_engines.py -v shows 16/16 tests PASSED with zero failures.`nPHASE 3Networking & Secure Protocol`nDuration: Day 6-8  •  Objective: Encrypted messages flying over a real TCP socket between sender and receiver`n3.1 Packet Serializer/DeserializerCreate src/network/packet.py. This handles converting between our secure packet format and raw bytes:import structclass SecurePacket:    VERSION = 0x01    def __init__(self, algo_id, nonce, seq_num, timestamp, ad, ciphertext, tag):        self.algo_id = algo_id        self.nonce = nonce        # 16 bytes        self.seq_num = seq_num    # uint32        self.timestamp = timestamp  # uint64 (ms)        self.ad = ad              # variable        self.ciphertext = ciphertext  # variable        self.tag = tag            # 16 bytes    def serialize(self) -> bytes:        header = struct.pack('>BB', self.VERSION, self.algo_id)        header += self.nonce        header += struct.pack('>I', self.seq_num)        header += struct.pack('>Q', self.timestamp)        header += struct.pack('>H', len(self.ad)) + self.ad        header += struct.pack('>H', len(self.ciphertext)) + self.ciphertext        header += self.tag        return header    @staticmethod    def deserialize(data: bytes) -> 'SecurePacket':        offset = 0        version, algo_id = struct.unpack_from('>BB', data, offset); offset += 2        nonce = data[offset:offset+16]; offset += 16        seq_num = struct.unpack_from('>I', data, offset)[0]; offset += 4        timestamp = struct.unpack_from('>Q', data, offset)[0]; offset += 8        ad_len = struct.unpack_from('>H', data, offset)[0]; offset += 2        ad = data[offset:offset+ad_len]; offset += ad_len        ct_len = struct.unpack_from('>H', data, offset)[0]; offset += 2        ct = data[offset:offset+ct_len]; offset += ct_len        tag = data[offset:offset+16]        return SecurePacket(algo_id, nonce, seq_num, timestamp, ad, ct, tag)`n3.2 Replay GuardCreate src/network/replay_guard.py:class ReplayGuard:    '''Sliding-window anti-replay (RFC 4303 style)'''    def __init__(self, window_size=64):        self.window_size = window_size        self.highest_seq = 0        self.bitmap = 0  # 64-bit bitmap    def check_and_update(self, seq_num: int) -> bool:        '''Returns True if packet is fresh, False if replay.'''        if seq_num == 0:            return False        if seq_num > self.highest_seq:            shift = seq_num - self.highest_seq            if shift >= self.window_size:                self.bitmap = 1            else:                self.bitmap = (self.bitmap << shift) | 1            self.highest_seq = seq_num            return True        diff = self.highest_seq - seq_num        if diff >= self.window_size:            return False  # Too old        bit = 1 << diff        if self.bitmap & bit:            return False  # Already seen (replay!)        self.bitmap |= bit        return True`n3.3 Sender NodeCreate src/network/sender.py:import socket, struct, os, timefrom src.network.packet import SecurePacketclass SecureSender:    def __init__(self, host, port, engine, key):        self.host = host        self.port = port        self.engine = engine        self.key = key        self.seq_num = 0    def send(self, message: str, device_id: str = 'SENDER01'):        self.seq_num += 1        nonce = struct.pack('>Q', self.seq_num) + os.urandom(8)        timestamp = int(time.time() * 1000)        ad = device_id.encode() + struct.pack('>Q', timestamp)        plaintext = message.encode('utf-8')        ct, tag = self.engine.encrypt(self.key, nonce, ad, plaintext)        pkt = SecurePacket(self.engine.algo_id, nonce, self.seq_num, timestamp, ad, ct, tag)        raw = pkt.serialize()        with socket.socket(socket.AF_INET, socket.SOCK_STREAM) as s:            s.connect((self.host, self.port))            s.sendall(struct.pack('>I', len(raw)) + raw)  # Length-prefixed            print(f'[SENDER] Sent message #{self.seq_num} ({len(raw)} bytes)')            return raw  # Return for testing/capture`n3.4 Receiver NodeCreate src/network/receiver.py:import socket, structfrom src.network.packet import SecurePacketfrom src.network.replay_guard import ReplayGuardclass SecureReceiver:    def __init__(self, port, engine, key):        self.port = port        self.engine = engine        self.key = key        self.replay_guard = ReplayGuard()    def listen_once(self):        '''Listen for one message, return (plaintext, status)'''        with socket.socket(socket.AF_INET, socket.SOCK_STREAM) as s:            s.setsockopt(socket.SOL_SOCKET, socket.SO_REUSEADDR, 1)            s.bind(('0.0.0.0', self.port))            s.listen(1)            print(f'[RECEIVER] Listening on port {self.port}...')            conn, addr = s.accept()            with conn:                length_data = conn.recv(4)                msg_len = struct.unpack('>I', length_data)[0]                data = b''                while len(data) < msg_len:                    data += conn.recv(msg_len - len(data))                pkt = SecurePacket.deserialize(data)                # Replay check                if not self.replay_guard.check_and_update(pkt.seq_num):                    print(f'[RECEIVER] REPLAY DETECTED! seq={pkt.seq_num}')                    return None, 'REPLAY_REJECTED'                # Decrypt and verify                pt = self.engine.decrypt(self.key, pkt.nonce, pkt.ad, pkt.ciphertext, pkt.tag)                if pt is None:                    print(f'[RECEIVER] AUTH FAILURE! Tag verification failed.')                    return None, 'AUTH_FAILURE'                msg = pt.decode('utf-8')                print(f'[RECEIVER] Decrypted: "{msg}" (seq={pkt.seq_num})')                return msg, 'OK'`n3.5 End-to-End Test ScriptCreate tests/test_e2e.py to verify everything works together:import threading, time, osfrom src.crypto.ascon_engine import AsconEnginefrom src.network.sender import SecureSenderfrom src.network.receiver import SecureReceiverPORT = 9999KEY = os.urandom(16)engine = AsconEngine()result = {}def run_receiver():    rx = SecureReceiver(PORT, engine, KEY)    result['msg'], result['status'] = rx.listen_once()t = threading.Thread(target=run_receiver)t.start()time.sleep(0.5)  # Let receiver starttx = SecureSender('127.0.0.1', PORT, engine, KEY)tx.send('Hello from ASCON!')t.join()assert result['status'] == 'OK'assert result['msg'] == 'Hello from ASCON!'print('END-TO-END TEST PASSED!')3.6 `nPhase 3 ChecklistDoneTaskFile(s) to CreateVerification☐Implement SecurePacket serialize/deserializesrc/network/packet.pyRound-trip test: serialize then deserialize recovers all fields☐Implement ReplayGuard sliding windowsrc/network/replay_guard.pyAccepts fresh, rejects duplicate, rejects old☐Implement SecureSendersrc/network/sender.pySends packet over TCP without error☐Implement SecureReceiversrc/network/receiver.pyReceives, decrypts, and prints plaintext☐End-to-end test with ASCON enginetests/test_e2e.pyPrints END-TO-END TEST PASSED☐Test with each of the 4 engines-All 4 engines work end-to-end☐Test replay rejection-Second send of same packet prints REPLAY DETECTED☐Test tampered packet rejection-Modified packet prints AUTH FAILURE`nExit Gate: Run tests/test_e2e.py. Console shows sender encrypting, receiver decrypting, and the message matches. Then manually test replay rejection.`nPHASE 4Benchmarking Suite`nDuration: Day 9-11  •  Objective: Generate hard data comparing all 4 algorithms across multiple payload sizes`n4.1 Benchmark RunnerCreate src/benchmark/bench_runner.py:import os, time, tracemalloc, csvfrom src.crypto.ascon_engine import AsconEnginefrom src.crypto.aes_engine import AESEnginefrom src.crypto.speck_engine import SpeckEnginefrom src.crypto.present_engine import PresentEngineENGINES = [AsconEngine(), AESEngine(), SpeckEngine(), PresentEngine()]PAYLOAD_SIZES = [16, 64, 256, 1024, 4096, 16384]ITERATIONS = {16: 10000, 64: 10000, 256: 5000, 1024: 2000, 4096: 1000, 16384: 500}def run_benchmarks(output_csv='results/benchmark_results.csv'):    results = []    key = os.urandom(16)    nonce = os.urandom(16)    ad = b'benchmark-associated-data'    for engine in ENGINES:        for size in PAYLOAD_SIZES:            plaintext = os.urandom(size)            iters = ITERATIONS[size]            # --- Encryption benchmark ---            tracemalloc.start()            enc_times = []            for _ in range(iters):                start = time.perf_counter_ns()                ct, tag = engine.encrypt(key, nonce, ad, plaintext)                end = time.perf_counter_ns()                enc_times.append(end - start)            _, enc_peak_mem = tracemalloc.get_traced_memory()            tracemalloc.stop()            # --- Decryption benchmark ---            tracemalloc.start()            dec_times = []            for _ in range(iters):                start = time.perf_counter_ns()                engine.decrypt(key, nonce, ad, ct, tag)                end = time.perf_counter_ns()                dec_times.append(end - start)            _, dec_peak_mem = tracemalloc.get_traced_memory()            tracemalloc.stop()            avg_enc = sum(enc_times) / len(enc_times) / 1000  # microseconds            avg_dec = sum(dec_times) / len(dec_times) / 1000            throughput = (size / (avg_enc / 1e6)) / 1024  # KB/s            row = {                'algorithm': engine.name(),                'payload_bytes': size,                'iterations': iters,                'avg_encrypt_us': round(avg_enc, 2),                'avg_decrypt_us': round(avg_dec, 2),                'throughput_kbps': round(throughput, 2),                'enc_peak_mem_kb': round(enc_peak_mem / 1024, 2),                'dec_peak_mem_kb': round(dec_peak_mem / 1024, 2),            }            results.append(row)            print(f'{engine.name():25s} | {size:6d}B | Enc: {avg_enc:10.2f}us | {throughput:10.2f} KB/s')    # Write CSV    os.makedirs('results', exist_ok=True)    with open(output_csv, 'w', newline='') as f:        writer = csv.DictWriter(f, fieldnames=results[0].keys())        writer.writeheader()        writer.writerows(results)    print(f'Results saved to {output_csv}')if __name__ == '__main__':    run_benchmarks()4.2 `nPhase 4 ChecklistDoneTaskFile(s) to CreateVerification☐Create benchmark runner with timing loopsrc/benchmark/bench_runner.pyPrints per-algorithm per-size results☐Add memory tracking with tracemalloc-Peak memory column populated in CSV☐Run full benchmark suite-Console shows all 24 rows (4 algos x 6 sizes)☐Export results to CSVresults/benchmark_results.csvCSV file exists with all columns☐Verify results are reasonable (no zeros, no negatives)-Manual inspection of CSV☐Run benchmark 3 times for consistencyresults/run1.csv, run2.csv, run3.csvValues within 10-15% across runs`nExit Gate: results/benchmark_results.csv exists with 24 rows of data. Throughput values are positive and reasonable.`nPHASE 5Attack Simulations`nDuration: Day 12-14  •  Objective: Demonstrate 3 wireless attacks and prove the system defends against each`n5.1 Eavesdropping DemoCreate src/attacks/eavesdrop_demo.py. This runs sender and receiver, with a third thread capturing raw bytes:import threading, time, osfrom src.crypto.ascon_engine import AsconEnginefrom src.network.sender import SecureSenderfrom src.network.receiver import SecureReceiverKEY = os.urandom(16)PORT = 9998SECRET_MSG = 'Top secret sensor reading: `n4`n2.7 degrees'captured_data = {}def run_receiver():    rx = SecureReceiver(PORT, AsconEngine(), KEY)    msg, status = rx.listen_once()    captured_data['decrypted'] = msgdef run_sender():    time.sleep(0.5)    tx = SecureSender('127.0.0.1', PORT, AsconEngine(), KEY)    raw = tx.send(SECRET_MSG)    captured_data['raw_packet'] = rawt_rx = threading.Thread(target=run_receiver)t_tx = threading.Thread(target=run_sender)t_rx.start(); t_tx.start()t_rx.join(); t_tx.join()print('\n' + '='*60)print('EAVESDROPPING ANALYSIS')print('='*60)print(f'Original message:  {SECRET_MSG}')print(f'Captured raw hex:  {captured_data["raw_packet"].hex()[:120]}...')print(f'Plaintext in raw?: {SECRET_MSG.encode() in captured_data["raw_packet"]}')print(f'Decrypted at RX:   {captured_data["decrypted"]}')print('\nRESULT: Eavesdropper sees ONLY encrypted bytes. NO plaintext leakage.')`n5.2 Replay Attack DemoCreate src/attacks/replay_demo.py:import socket, struct, threading, time, osfrom src.crypto.ascon_engine import AsconEnginefrom src.network.sender import SecureSenderfrom src.network.receiver import SecureReceiverKEY = os.urandom(16)PORT = 9997results = []def receiver_loop(count):    '''Receive multiple messages'''    engine = AsconEngine()    from src.network.replay_guard import ReplayGuard    guard = ReplayGuard()    with socket.socket(socket.AF_INET, socket.SOCK_STREAM) as s:        s.setsockopt(socket.SOL_SOCKET, socket.SO_REUSEADDR, 1)        s.bind(('0.0.0.0', PORT)); s.listen(5)        for _ in range(count):            conn, addr = s.accept()            with conn:                length_data = conn.recv(4)                msg_len = struct.unpack('>I', length_data)[0]                data = b''                while len(data) < msg_len: data += conn.recv(msg_len - len(data))                from src.network.packet import SecurePacket                pkt = SecurePacket.deserialize(data)                if not guard.check_and_update(pkt.seq_num):                    results.append('REPLAY_REJECTED')                    continue                pt = engine.decrypt(KEY, pkt.nonce, pkt.ad, pkt.ciphertext, pkt.tag)                results.append('OK' if pt else 'AUTH_FAIL')t = threading.Thread(target=receiver_loop, args=(3,))t.start(); time.sleep(0.5)# Send legitimate messagetx = SecureSender('127.0.0.1', PORT, AsconEngine(), KEY)captured_raw = tx.send('Transfer $1000 to Account X')time.sleep(0.3)# ATTACKER: Replay the exact same raw packet twicefor i in range(2):    with socket.socket(socket.AF_INET, socket.SOCK_STREAM) as s:        s.connect(('127.0.0.1', PORT))        s.sendall(struct.pack('>I', len(captured_raw)) + captured_raw)    time.sleep(0.2)t.join()print('\n' + '='*60)print('REPLAY ATTACK ANALYSIS')print('='*60)print(f'Message 1 (legitimate): {results[0]}')print(f'Message 2 (replay #1):  {results[1]}')print(f'Message 3 (replay #2):  {results[2]}')print('\nRESULT: First message accepted, both replays REJECTED.')`n5.3 MITM / Tamper DemoCreate src/attacks/mitm_demo.py. Simulate an attacker flipping bits in ciphertext:import os, threading, time, socket, structfrom src.crypto.ascon_engine import AsconEnginefrom src.network.sender import SecureSenderfrom src.network.receiver import SecureReceiverKEY = os.urandom(16)PORT = 9996# Step 1: Capture a legitimate packettx = SecureSender('127.0.0.1', PORT, AsconEngine(), KEY)# ... (start receiver in thread, capture raw bytes from sender)# Step 2: Tamper - flip 10 random bytes in the ciphertext portiontampered = bytearray(captured_raw)for i in range(10):    pos = 30 + i  # Offset into ciphertext area    tampered[pos] ^= 0xFF# Step 3: Send tampered packet to receiver# ... receiver should print AUTH FAILUREprint('\nRESULT: Tampered packet REJECTED. 128-bit auth tag detects modification.')5.4 `nPhase 5 ChecklistDoneTaskFile(s) to CreateVerification☐Eavesdrop demo: plaintext NOT in captured bytessrc/attacks/eavesdrop_demo.pyOutput shows False for plaintext-in-raw check☐Replay demo: first accepted, replays rejectedsrc/attacks/replay_demo.pyOutput shows OK, REPLAY_REJECTED, REPLAY_REJECTED☐MITM demo: tampered packet rejectedsrc/attacks/mitm_demo.pyOutput shows AUTH FAILURE☐Screenshot/log each demo outputresults/attack_logs/3 log files or screenshots saved`nExit Gate: All 3 attack scripts run successfully and produce the expected defense output.`nPHASE 6Analysis, Visualization & Report`nDuration: Day 15-18  •  Objective: Transform raw data into professional charts and a complete project report`n6.1 Visualization ScriptCreate src/benchmark/visualize.py to generate all charts from the CSV:import pandas as pdimport matplotlib.pyplot as pltimport seaborn as snsdf = pd.read_csv('results/benchmark_results.csv')sns.set_theme(style='whitegrid', palette='deep')# Chart 1: Throughput vs Payload Size (MOST IMPORTANT CHART)fig, ax = plt.subplots(figsize=(12, 6))for algo in df['algorithm'].unique():    subset = df[df['algorithm'] == algo]    ax.plot(subset['payload_bytes'], subset['throughput_kbps'], marker='o', label=algo, linewidth=2)ax.set_xlabel('Payload Size (bytes)', fontsize=12)ax.set_ylabel('Throughput (KB/s)', fontsize=12)ax.set_title('Encryption Throughput Comparison', fontsize=14, fontweight='bold')ax.set_xscale('log', base=2)ax.legend(fontsize=11)plt.tight_layout()plt.savefig('results/chart1_throughput.png', dpi=150)print('Chart 1 saved.')# Chart 2: Encryption Time vs Payload Sizefig, ax = plt.subplots(figsize=(12, 6))for algo in df['algorithm'].unique():    subset = df[df['algorithm'] == algo]    ax.bar_label(ax.bar(... ))  # Use grouped bar chart# ... similar pattern for each chart# Chart 3: Memory Usage (bar chart at 1KB payload)# Chart 4: Encryption vs Decryption Time comparison# Chart 5: Security Level vs Throughput scatter# Chart 6: Attack Detection Summary`n6.2 Charts to GenerateChart #TypeX-AxisY-AxisPurpose1Line PlotPayload Size (log2)Throughput (KB/s)Core performance comparison2Grouped BarAlgorithmEncryption Time (us)Speed at specific sizes (16B, 1KB, 16KB)3Horizontal BarAlgorithmPeak Memory (KB)RAM constraint suitability4Stacked BarAlgorithmTime (us)Encryption vs Decryption breakdown5ScatterThroughputSecurity (bits)Trade-off visualization6Bar ChartAttack TypeDetection %Security effectiveness summary`n6.3 Final Report StructureYour final project report should follow this structure:SectionContentApprox Pages1. Abstract150-word summary of project, methods, and key finding0.52. IntroductionProblem context, motivation, objectives1.53. Literature ReviewLightweight crypto evolution, NIST process, ASCON34. System DesignArchitecture, packet format, flow diagrams (from main doc)35. ImplementationTech stack, code structure, key code snippets46. Benchmarking ResultsAll 6 charts with analysis paragraphs47. Security AnalysisAttack demos, defense proofs, security properties table38. ConclusionKey findings, ASCON advantages, future work19. References15-20 citations in IEEE format16.4 `nPhase 6 ChecklistDoneTaskFile(s) to CreateVerification☐Generate Chart 1: Throughput comparisonresults/chart1_throughput.pngLine chart shows 4 algorithms across 6 sizes☐Generate Chart 2: Encryption time barsresults/chart2_enc_time.pngGrouped bar chart is readable☐Generate Chart 3: Memory comparisonresults/chart3_memory.pngBar chart shows relative memory usage☐Generate Chart 4: Encrypt vs Decryptresults/chart4_enc_dec.pngStacked bars for each algorithm☐Generate Chart 5: Security vs Performanceresults/chart5_tradeoff.pngScatter plot with labeled points☐Generate Chart 6: Attack detectionresults/chart6_attacks.png100% detection for all 3 attacks☐Write final report (20+ pages)docs/Final_Report.docxAll sections complete with charts embedded☐Create presentation (10-12 slides)docs/Presentation.pptxCovers problem, design, results, demo`nExit Gate: 6 PNG charts exist in results/. Final report has all sections. You can explain every chart and defend every design decision.Quick Reference: Common CommandsWhatCommandActivate virtual environmentsource venv/bin/activateRun all crypto testspytest tests/test_crypto_engines.py -vRun end-to-end testpython tests/test_e2e.pyRun full benchmarkpython -m src.benchmark.bench_runnerRun eavesdrop demopython -m src.attacks.eavesdrop_demoRun replay demopython -m src.attacks.replay_demoRun MITM demopython -m src.attacks.mitm_demoGenerate all chartspython -m src.benchmark.visualizeInstall new dependencypip install <package> && pip freeze > requirements.txtQuick ASCON testpython -c "import ascon; print(ascon.encrypt(b'\x00'*16, b'\x00'*16, b'', b'test').hex())"
